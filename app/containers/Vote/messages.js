/*
 * Vote Messages
 *
 * This contains all the text for the Vote container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.Vote";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the Vote container!"
  }
});

// import { selectDietaryRequirementsDomain } from '../selectors';

describe("selectDietaryRequirementsDomain", () => {
  it("Expect to have unit tests specified", () => {
    expect(true).toEqual(false);
  });
});

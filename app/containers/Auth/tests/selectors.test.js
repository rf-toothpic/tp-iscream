import { selectAuthDomain } from '../selectors'

describe('selectAuthDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true)
    console.warn('tests not specified')
  })
})

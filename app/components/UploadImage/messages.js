/*
 * UploadImage Messages
 *
 * This contains all the text for the UploadImage component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.UploadImage";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the UploadImage component!"
  }
});
